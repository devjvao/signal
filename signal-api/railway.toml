[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
preDeployCommand = "migrate -path db/migrations -database $DB_URL up"
startCommand = "/app/api"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
